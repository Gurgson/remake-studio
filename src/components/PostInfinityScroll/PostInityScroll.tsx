"use client"
import React, { FC } from 'react'
interface IProps {
    props: {
        initialPosts: []
    }
}
const PostInityScroll: FC<IProps> = ({props}) => {
  return (
    <div>PostInityScroll</div>
  )
}

export default PostInityScroll