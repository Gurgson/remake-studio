import { Metadata } from 'next'
import React from 'react'
export const metadata : Metadata = {
  title: `${process.env.WEBSITE_NAME} - User Panel`,
}
const AccPage = () => {
  return (
    <></>
  )
}

export default AccPage